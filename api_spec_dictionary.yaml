---
# =============================================================================
# Dictionary for api_spec.yaml
# =============================================================================
# Specmatic auto-detects this file (<spec-name>_dictionary.yaml) and uses
# these values instead of random data during contract tests and stubs.
# Only ACTIVE (uncommented) entries are used at runtime.
# =============================================================================

# --- OAuth2 Login Form (application/x-www-form-urlencoded) ---
Body_login_for_access_token_auth_token_post:
  username: "test_user"
  password: "SecureP@ssw0rd!"
  grant_type: "password"
  scope: ""
  client_id: ""
  client_secret: ""

# --- Token Response ---
# A long-lived test JWT signed with the backend's secret.
# Used by Specmatic for every authenticated example request.
Token:
  access_token: "$(JWT_TOKEN)"
  token_type: "bearer"
