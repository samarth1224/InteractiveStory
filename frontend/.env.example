# Use the absolute URL of the API for Next.js SSR (Server-Side Rendering) 
# and client fetching. If using Nginx reverse proxy, point to the deployed domain.
NEXT_PUBLIC_API_URL="https://yourdomain.com/api"
